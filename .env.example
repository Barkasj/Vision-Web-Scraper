# Backend Configuration
# GO_ENV=development
# API_PORT=8080

# Neo4j Connection
NEO4J_URI=neo4j://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password # IMPORTANT: Change this in your actual .env file

# Renderer (Playwright) Configuration
# RENDERER_PORT=3000 # If the renderer runs as a separate service with a port
# PLAYWRIGHT_BROWSER=chromium # Options: chromium, firefox, webkit

# CV Service Configuration
# CV_SERVICE_PORT=5000 # If the CV service runs as a separate service with a port
# MODEL_PATH=/app/models # Example path if models are mounted in Docker

# Optional: Cloud Service API Keys (if used for OCR, etc.)
# GOOGLE_APPLICATION_CREDENTIALS=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# Proxy Configuration (if needed for scraping)
# HTTP_PROXY=
# HTTPS_PROXY=
